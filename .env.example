# Cangjie MCP Server Configuration
# Copy this file to .env and fill in your values
#
# All options can also be set via CLI arguments.
# Run `cangjie-mcp --help` to see all options and their environment variables.

# ===========================================
# Cangjie Documentation Settings
# ===========================================

# Documentation version (git tag, default: latest)
CANGJIE_DOCS_VERSION=latest

# Documentation language: zh (Chinese) or en (English)
CANGJIE_DOCS_LANG=zh

# Data directory path (default: ~/.cangjie-mcp)
CANGJIE_DATA_DIR=./data

# Max chunk size in characters (default: 6000)
CANGJIE_CHUNK_MAX_SIZE=6000

# Remote server URL (optional, forwards queries to an HTTP server)
# CANGJIE_SERVER_URL=http://localhost:8765

# ===========================================
# Embedding Settings
# ===========================================

# Embedding model type: local or openai
CANGJIE_EMBEDDING_TYPE=openai

# Local HuggingFace model name (used when CANGJIE_EMBEDDING_TYPE=local)
# CANGJIE_LOCAL_MODEL=paraphrase-multilingual-MiniLM-L12-v2

# ===========================================
# OpenAI Settings (required when CANGJIE_EMBEDDING_TYPE=openai)
# ===========================================

# OpenAI API Key (SiliconFlow)
OPENAI_API_KEY=your-siliconflow-api-key-here

# OpenAI API Base URL (SiliconFlow)
OPENAI_BASE_URL=https://api.siliconflow.cn/v1

# OpenAI embedding model
OPENAI_EMBEDDING_MODEL=BAAI/bge-m3

# ===========================================
# Rerank Settings (optional)
# ===========================================

# Rerank type: none, local, or openai (default: none)
CANGJIE_RERANK_TYPE=none

# Rerank model name
CANGJIE_RERANK_MODEL=BAAI/bge-reranker-v2-m3

# Number of results after reranking (default: 5)
CANGJIE_RERANK_TOP_K=5

# Number of candidates before reranking (default: 20)
CANGJIE_RERANK_INITIAL_K=20

# ===========================================
# HTTP Server Settings (for `cangjie-mcp server`)
# ===========================================

# HTTP server host address (default: 127.0.0.1)
CANGJIE_SERVER_HOST=127.0.0.1

# HTTP server port (default: 8765)
CANGJIE_SERVER_PORT=8765
